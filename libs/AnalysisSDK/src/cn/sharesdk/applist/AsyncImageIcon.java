package cn.sharesdk.applist;

import android.content.Context;
import android.util.AttributeSet;
import m.framework.ui.widget.asyncview.AsyncImageView;

public class AsyncImageIcon extends AsyncImageView{
	
	public AsyncImageIcon(Context context) {
		super(context);
	}
	
	public AsyncImageIcon(Context context, AttributeSet attrs) {
		super(context, attrs);
	}
	
	public AsyncImageIcon(Context context, AttributeSet attrs, int defStyle) {
		super(context, attrs, defStyle);
	}
}
